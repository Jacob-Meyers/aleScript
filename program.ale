var?runFunc?"false

var?funcLN?THIS_LINE
if?runFunc?"!=?"false?"0?"skipTo

println?"function

var?runFunc?"false
return?"empty
-<skipTo

@REP_IGNORE
input?"~> ?
if?LAST_RETURNED?"==?"a?"0?"skipTo
var?runFunc?"true
setln?funcLN
-<skipTo
@REP
